import React, { Component } from 'react';
import SiteContainer from './SiteContainer';

class App extends Component {
  render() {
    return (
      <SiteContainer />
    );
  }
}

export default App;
